

var canvas = document.getElementById("maze_canvas");



//window.addEventListener('load', ()=> {
        //document.addEventListener('keydown', process_keydown);
        //document.addEventListener('keyup', process_keyup);
        //fetch('hypertiles.json')
        //        .then(response => response.json()) // Parse JSON
        //        .then(data => {tiles = data; console.log("yay"); requestAnimationFrame(frame);}) // Work with JSON data
        //        .catch(error => {console.error('Error fetching JSON:', error);});

//})


